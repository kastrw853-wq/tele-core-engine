# الأساسيات والمحرك الرئيسي
pyTelegramBotAPI==4.10.0
Flask==2.2.2
gunicorn==20.1.0

# التعامل مع البيئة والملفات (env)
python-dotenv==1.0.0

# الربط وطلب البيانات (لخوارزمية QR والدردشات)
requests==2.28.2
urllib3==1.26.15

# التعامل مع البيانات المتقدمة (لمجلد vault)
cryptography==40.0.1
PyYAML==6.0

# معالجة الصور (في حال التقاط سكرين شوت مستقبلاً)
Pillow==9.4.0

# الأمان والمزامنة
asyncio==3.4.3
